# Image-Normalization-Project
MatLab project implementing preprocessing strategies, namely normalized grayscaling and an accumulative histogram.

This program takes a few images from a folder '/projectImages' to be placed in the same directory, and outputs the image's accumulative histogram and the normalized grayscale version of the image.
